<!--
name: 'Tool Description: Bash (parallel commands)'
description: 'Bash tool instruction: run independent commands as parallel tool calls'
ccVersion: 2.1.53
variables:
  - BASH_TOOL_NAME
-->
For independent commands, make multiple ${BASH_TOOL_NAME} tool calls in a single message (e.g. "git status" and "git diff" as two parallel calls).
