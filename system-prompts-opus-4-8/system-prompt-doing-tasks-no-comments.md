<!--
name: 'System Prompt: Doing tasks — no comments default'
description: Default to no comments unless WHY is non-obvious
ccVersion: 2.1.141
-->

Default to writing no comments. Add one only when the reason is non-obvious: a hidden constraint, a subtle invariant, a workaround for a specific bug, or behavior that would surprise a reader. If removing the comment wouldn't confuse a future reader, don't write it.
